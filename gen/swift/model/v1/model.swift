// Code generated by protoc-gen-swift-codable. DO NOT EDIT.
// source: model/v1/model.proto

public enum Modality: String, Codable, Sendable {
    case modalityUnspecified = "MODALITY_UNSPECIFIED"
    case modalityText = "MODALITY_TEXT"
    case modalityAudio = "MODALITY_AUDIO"
    case modalityImage = "MODALITY_IMAGE"

    // The proto3 default (value 0); used when the field is absent on the wire.
    public static let `default` = Modality.modalityUnspecified
}

public enum Architecture: String, Codable, Sendable {
    case architectureUnspecified = "ARCHITECTURE_UNSPECIFIED"
    case architectureDecoder = "ARCHITECTURE_DECODER"
    case architectureEncoderDecoder = "ARCHITECTURE_ENCODER_DECODER"
    case architectureEncoder = "ARCHITECTURE_ENCODER"

    // The proto3 default (value 0); used when the field is absent on the wire.
    public static let `default` = Architecture.architectureUnspecified
}

public enum Role: String, Codable, Sendable {
    case roleUnspecified = "ROLE_UNSPECIFIED"
    case roleChat = "ROLE_CHAT"
    case roleCompletion = "ROLE_COMPLETION"
    case roleEmbedding = "ROLE_EMBEDDING"
    case roleTranscription = "ROLE_TRANSCRIPTION"
    case roleAudioAnnotation = "ROLE_AUDIO_ANNOTATION"
    case roleImageGeneration = "ROLE_IMAGE_GENERATION"
    case roleSpeechSynthesis = "ROLE_SPEECH_SYNTHESIS"

    // The proto3 default (value 0); used when the field is absent on the wire.
    public static let `default` = Role.roleUnspecified
}

public enum Provider: String, Codable, Sendable {
    case providerUnspecified = "PROVIDER_UNSPECIFIED"
    case providerOllama = "PROVIDER_OLLAMA"
    case providerInProcess = "PROVIDER_IN_PROCESS"
    case providerOpenaiCompatible = "PROVIDER_OPENAI_COMPATIBLE"
    case providerAnthropic = "PROVIDER_ANTHROPIC"
    case providerComfyui = "PROVIDER_COMFYUI"
    case providerAppleOnDevice = "PROVIDER_APPLE_ON_DEVICE"

    // The proto3 default (value 0); used when the field is absent on the wire.
    public static let `default` = Provider.providerUnspecified
}

public struct ModelDescriptor: Codable, Sendable {
    public var name: String
    public var modality: Modality
    public var architecture: Architecture
    public var role: Role
    public var provider: Provider
    public var endpoint: String
    public var modelId: String
    public var authSecret: String

    public init(
        name: String = "",
        modality: Modality = .default,
        architecture: Architecture = .default,
        role: Role = .default,
        provider: Provider = .default,
        endpoint: String = "",
        modelId: String = "",
        authSecret: String = ""
    ) {
        self.name = name
        self.modality = modality
        self.architecture = architecture
        self.role = role
        self.provider = provider
        self.endpoint = endpoint
        self.modelId = modelId
        self.authSecret = authSecret
    }

    public init(from decoder: any Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try c.decodeIfPresent(String.self, forKey: .name) ?? ""
        self.modality = try c.decodeIfPresent(Modality.self, forKey: .modality) ?? .default
        self.architecture = try c.decodeIfPresent(Architecture.self, forKey: .architecture) ?? .default
        self.role = try c.decodeIfPresent(Role.self, forKey: .role) ?? .default
        self.provider = try c.decodeIfPresent(Provider.self, forKey: .provider) ?? .default
        self.endpoint = try c.decodeIfPresent(String.self, forKey: .endpoint) ?? ""
        self.modelId = try c.decodeIfPresent(String.self, forKey: .modelId) ?? ""
        self.authSecret = try c.decodeIfPresent(String.self, forKey: .authSecret) ?? ""
    }

    enum CodingKeys: String, CodingKey {
        case name
        case modality
        case architecture
        case role
        case provider
        case endpoint
        case modelId
        case authSecret
    }
}

public struct ModelFamily: Codable, Sendable {
    public var name: String
    public var description: String
    public var modality: Modality
    public var role: Role
    public var members: [ModelDescriptor]
    public var defaultMember: String

    public init(
        name: String = "",
        description: String = "",
        modality: Modality = .default,
        role: Role = .default,
        members: [ModelDescriptor] = [],
        defaultMember: String = ""
    ) {
        self.name = name
        self.description = description
        self.modality = modality
        self.role = role
        self.members = members
        self.defaultMember = defaultMember
    }

    public init(from decoder: any Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try c.decodeIfPresent(String.self, forKey: .name) ?? ""
        self.description = try c.decodeIfPresent(String.self, forKey: .description) ?? ""
        self.modality = try c.decodeIfPresent(Modality.self, forKey: .modality) ?? .default
        self.role = try c.decodeIfPresent(Role.self, forKey: .role) ?? .default
        self.members = try c.decodeIfPresent([ModelDescriptor].self, forKey: .members) ?? []
        self.defaultMember = try c.decodeIfPresent(String.self, forKey: .defaultMember) ?? ""
    }

    enum CodingKeys: String, CodingKey {
        case name
        case description
        case modality
        case role
        case members
        case defaultMember
    }
}

public struct ModelCatalog: Codable, Sendable {
    public var families: [ModelFamily]

    public init(
        families: [ModelFamily] = []
    ) {
        self.families = families
    }

    public init(from decoder: any Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.families = try c.decodeIfPresent([ModelFamily].self, forKey: .families) ?? []
    }

    enum CodingKeys: String, CodingKey {
        case families
    }
}

public struct InvokeRequest: Codable, Sendable {
    public var role: Role
    public var modelId: String
    public var text: String
    public var inputPath: String
    public var params: [String: String]

    public init(
        role: Role = .default,
        modelId: String = "",
        text: String = "",
        inputPath: String = "",
        params: [String: String] = [:]
    ) {
        self.role = role
        self.modelId = modelId
        self.text = text
        self.inputPath = inputPath
        self.params = params
    }

    public init(from decoder: any Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.role = try c.decodeIfPresent(Role.self, forKey: .role) ?? .default
        self.modelId = try c.decodeIfPresent(String.self, forKey: .modelId) ?? ""
        self.text = try c.decodeIfPresent(String.self, forKey: .text) ?? ""
        self.inputPath = try c.decodeIfPresent(String.self, forKey: .inputPath) ?? ""
        self.params = try c.decodeIfPresent([String: String].self, forKey: .params) ?? [:]
    }

    enum CodingKeys: String, CodingKey {
        case role
        case modelId
        case text
        case inputPath
        case params
    }
}

public struct InvokeResponse: Codable, Sendable {
    public var text: String
    public var outputPath: String
    public var detail: [String: String]

    public init(
        text: String = "",
        outputPath: String = "",
        detail: [String: String] = [:]
    ) {
        self.text = text
        self.outputPath = outputPath
        self.detail = detail
    }

    public init(from decoder: any Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.text = try c.decodeIfPresent(String.self, forKey: .text) ?? ""
        self.outputPath = try c.decodeIfPresent(String.self, forKey: .outputPath) ?? ""
        self.detail = try c.decodeIfPresent([String: String].self, forKey: .detail) ?? [:]
    }

    enum CodingKeys: String, CodingKey {
        case text
        case outputPath
        case detail
    }
}

